Public Class EnhancementPlan
    Public Property Steps As List(Of String)  ' e.g., New List(Of String) From {"upscale", "denoise"}
    ' Expand with more properties as per spec (e.g., model parameters)
End Class